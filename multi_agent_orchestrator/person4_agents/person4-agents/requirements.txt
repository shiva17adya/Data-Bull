# Person 4 — multi-agent reasoning module
# Python 3.11+

pydantic>=2.5,<3.0
anthropic>=0.39           # optional at runtime: only used when USE_LLM=true
python-dotenv>=1.0        # optional: loads .env for local runs

# --- testing ---
pytest>=7.4
pytest-asyncio>=0.23
